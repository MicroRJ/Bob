# Self-hosting Bob

`bob.exe` is the known-good bootstrap binary used by `build.bat`.

Replace it only after the current `build\bob.exe` has successfully built Bob and passed the complete test suite.
