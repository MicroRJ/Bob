# Blessed Bob

`bob.exe` is the latest known-good bootstrap binary used by `build.bat`.

Run `build\bob.exe bless` only after the candidate has successfully built Bob
and passed the complete test suite.

The same binary is retained under `archive/<version>/bob.exe`.
